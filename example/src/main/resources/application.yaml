spring:
  datasource:
    url: "jdbc:postgresql://localhost:5433/fly_jpa"
    username: postgres
    password: postgres
  jpa:
    properties:
      org.hibernate.flushMode: COMMIT
      hibernate:
        format_sql: true
    show-sql: true
logging:
  level:
      org.hibernate.type.descriptor.sql.BasicBinder: trace
      root: debug
